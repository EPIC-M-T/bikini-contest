GitHub Pages deploy retry trigger.

The previous deploy failed at the final actions/deploy-pages publish step after artifact creation. This marker commit is intended to trigger a fresh Pages publish attempt.
