const nav=document.querySelector('#topNav'),toggle=document.querySelector('.mobile-toggle'),form=document.querySelector('#contestForm'),toast=document.querySelector('#toast'),overlay=document.querySelector('#heroGifOverlay'),photos=document.querySelector('#photos'),selected=document.querySelector('#selectedFiles'),prize=document.querySelector('.hero-prize'),cards=[...document.querySelectorAll('.asset-card')];toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');toggle.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.top-nav a,.footer a').forEach(a=>a.addEventListener('click',()=>{nav?.classList.remove('is-open');toggle?.setAttribute('aria-expanded','false')}));function showToast(m){if(!toast)return;toast.textContent=m;toast.classList.add('is-visible');setTimeout(()=>toast.classList.remove('is-visible'),3400)}photos?.addEventListener('change',()=>{if(selected)selected.textContent=(photos.files?.length||0)+' images selected.'});form?.addEventListener('submit',e=>{e.preventDefault();if(photos&&(photos.files?.length||0)<6){showToast('Please upload at least 6 images before submitting.');return}showToast('Thank you! Your EPIC Bikini Contest submission has been received for contestant review.')});function revealPrize(){prize?.classList.add('is-revealed')}addEventListener('load',()=>{if(overlay){setTimeout(()=>overlay.classList.add('is-hidden'),7000);setTimeout(revealPrize,7600)}else{setTimeout(revealPrize,600)}});if('IntersectionObserver'in window){const io=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');io.unobserve(entry.target)}})},{threshold:.22,rootMargin:'0px 0px -8% 0px'});cards.forEach(card=>io.observe(card))}else{cards.forEach(card=>card.classList.add('is-visible'))}