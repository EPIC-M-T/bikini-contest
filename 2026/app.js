const b=document.querySelector('.hamb'),m=document.querySelector('.menu');b?.addEventListener('click',()=>{const o=m.classList.toggle('open');b.setAttribute('aria-expanded',o)});m?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{m.classList.remove('open');b.setAttribute('aria-expanded','false')}));const s=document.querySelector('.site');function par(){s?.style.setProperty('--palmShift',Math.round(scrollY*.06)+'px')}addEventListener('scroll',par,{passive:true});par();document.querySelector('form')?.addEventListener('submit',e=>{e.preventDefault();alert('Demo mode: submission captured locally until connected to the CRM endpoint.')});